[package]
name = "quantum-ir"
version = "0.1.0"
edition = "2021"
description = "Quantum Runtime Intermediate Representation v0.1"

[dependencies]
# 无外部依赖，保持纯净

[features]
default = []
# 未来可扩展：serde 用于序列化
# serde = ["dep:serde"]
